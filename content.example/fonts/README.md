# fonts

Coloque aqui os arquivos de fonte (`*.woff2`) referenciados pelo seu CSS.
Ex.: `inter.woff2`, `inter-bold.woff2`.

Arquivos binários grandes ficam fora do Git — este exemplo é só a referência
da árvore de diretórios.
